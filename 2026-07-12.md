# Reading Log — 2026-07-12

- **14:43** [(20) Lindy on X: "Migrating from Claude to DeepSeek" / X](https://x.com/getlindy/status/2070214403576959008?s=46&t=YRszrDtk2QuNs_IUb5VqeA)
  - > dy's pricing only works if in
  - > s constraint. Lindy
  - > models would get cheaper fast enough that we could move users down the cost curve without making the product worse
  - > We moved most managed-agent model traffic, including Claude/Sonnet-backed paths and the remaining Gemini/Google paths, to DeepSeek v4 Flash on Atlas Cloud.
  - > models would get cheaper fast enough that we could move users down the cost curve without making the product worse.
  - > We moved most managed-agent model traffic, including Claude/Sonnet-backed paths and the remaining Gemini/Google paths, to DeepSeek v4 Flash on Atlas Cloud.
  - > infrastructure bill.
  - > Then we rolled it to a small slice of real usage and it failed the vibe test almost immediately
  - > ou need a way to replay real tasks and compare models without turning users into guinea pigs
  - > You tested a model, a provider, an inference stack, and your own prompts as one system.
  - > Then we tuned prompts until the offline scores roughly matched the old setup.
  - > First, online evals. These catch cases that the offline set misses, because production always invents weird inputs faster than you can write tests.
  - > he answer came from offline evals, provider testing, prompt optimization, internal rollout, online evals, retention, and finally a full ramp
  - > Move work to cheaper intelligence when the evidence says you can
- **14:48** [(20) Scott Wu on X: "Model routing is a hot topic but there are two challenges to doing routing for coding agents: 1) Even if different models can pass the task, there are subtle differences in behavior &amp; style that mean they aren't perfectly interchangeable. 2) The initial agent prompt isn't enough" / X](https://x.com/scottwu46/status/2071718682250928421?s=46&t=YRszrDtk2QuNs_IUb5VqeA)
  - > you need evals that account for style and behavior, not just pass/fail.
  - > you need the agent to be able to dynamically update and re-route.
  - > Well, you need evals that account for style and behavior, not just pass/fail. And you need the agent to be able to dynamically update and re-route.
- **14:49** [(20) Cognition on X: "Conventional model routing sucks. It passes benchmarks but fails to write code you'd actually merge. Introducing Devin Fusion, a new hybrid-model harness for agentic coding. In testing, it reduces the cost of Fable-level intelligence by 35% and still feels good to use. https://t.co/CLmDJ2tEcK" / X](https://x.com/cognition/status/2071624568465490170)
- **15:41** [(20) Michele Catasta on X: "Continual Learning for Agents" / X](https://x.com/pirroh/status/2074118901143679414?s=46&t=YRszrDtk2QuNs_IUb5VqeA)
  - > model, harness, and context [0]
  - > harness-level learning lets you mine production traces to systematically improve the code, tools, and instructions that power every instance of your agent, while context-level learning lets you personalize at the agent, user, and org level, so your product gets better with every interaction.
  - > evaluation must become part of the improvement loop.
  - > used to look like a one-way process: run the eval, produce a score, and make a shipping call.
  - > valuation had to move from launch check to improvement loop
  - > Offline benchmarks tell us whether candidate changes can complete simulated app-building tasks before we sh
  - > ow real users are affected after the changes ship. Those signals then flow back into evals and shipp
  - > Benchmarks catch regressions before release. A/B tests show whether production behavior moved. Trace clusters explain the failures under aggregate metrics
  - > our public benchmark for vibe coding
  - > equally flexible eval agen
  - > ask pairs the PRD with a set of natural-language test plans that describe the feature-level interactions and assertions the finished app must satis
  - > When you don't own the weights, you certainly can't fine-tune them.
  - > model, harness, and context [0]
  - > arness-level learning lets you mine production traces to systematically improve the code, tools, and instructions that power every instance of your agent, while context-level learning lets you personalize at the agent, user, and org level, so your product gets better with every interaction.
  - > Agent evaluation used to look like a one-way process: run the eval, produce a score, and make a shipping call.
  - > Evaluation had to move from launch check to improvement loop.
  - > The system has two measurement pillars and one optimization loop.
  - > Offline benchmarks tell us whether candidate changes can complete simulated app-building tasks before we ship them. Online A/B tests and production traces show how real users are affected after the changes ship.
  - > hose signals then flow back into evals and shippin
  - > Benchmarks catch regressions before release. A/B tests show whether production behavior moved. Trace clusters explain the failures under aggregate metrics. H
  - > they bring a product request. The agent chooses the stack, schema, routes, components, and interaction flows.
  - > For vibe coding, the evaluation target is the artifact itself: does it load, does the core workflow work,
  - > our public benchmark for vibe coding, measuring a simple but important signal: does the application built by the agent meet the spec?
  - > the same flexibility that makes ViBench realistic demands an equally flexible eval agent, one that stays grounded in the PRD.
  - > iBench task pairs the PRD with a set of natural-language test plans that describe the feature-level interactions and assertions the finished app must satisfy
  - > and our evals in general, at Replit scale also demands strong infrastructure support
  - > without risking cross-evaluation contamination
  - > ViBench foundation, a natural-language PRD graded by natural-language test plans, adapts to a range of vibe-coding scenarios.
  - > We have seen enough agent updates look good in controlled settings, only to regress real user behavior, to know that production needs its own measurement layer.
  - > we A/B most agent-affecting updates: prompts, tools, harness revisions, model swaps, and larger behavior changes.
  - > A/B tests surface user behavior, sentiment, and success
  - > elescope — our system for trace analysis and clustering — helps explain why
  - > Telescope uses short, evidence-grounded facets inspired by the same bottom-up approach as Cli
  - > ViBench, A/B tests, and Telescope can tell us what failed, where it failed, and how often it is happening
  - > The operating principle is simple: if agents are useful for building software, they should also be useful for improving the agent
  - > Then it builds a candidate, opens a draft PR with the reasoning attached, measures the result against ViBench, A/B results, trajectory data, and recent baselines, and recommends whether to ship, iterate, or drop it.
  - > engineers still review the result and own the launch decision.
  - > Each run records what it tried and what happened, including failures. That record improves the loop over time: future runs can reuse what worked, avoid known dead ends, and p
  - > an the candidate against ViBench to confirm that the happy path did not regress.
