# Reading Log — 2026-08-17

- **23:09** [A Field Guide to Rapidly Improving AI Products – Hamel’s Blog](https://hamel.dev/blog/posts/field-guide/#the-most-common-mistake-skipping-error-analysis)
  - > Error analysis - the single most valuable activity in AI development and consistently the highest-ROI activity
  - > a simple viewer to examine conversations between their AI and users.
  - > Instead of reaching for new tools, they: 1. Looked at actual conversation logs 2. Categorized the types of date-handling failures 3. Built specific tests to catch these issues 4. Measured improvement on these metrics
  - > The top-down approach starts with common metrics like “hallucination” or “toxicity” plus metrics unique to your task. While convenient, it often misses domain-specific issues.
  - > Then we used an LLM to build a taxonomy of common failure modes. Finally, we mapped each row to specific failure mode labels and counted the frequency of each issue.
  - > Here’s what makes a good data annotation tool:
  - > it’s building a customized interface that lets anyone examine what their AI is actually doing.
  - > Make feedback trivial to capture. One-click correct/incorrect buttons beat l
  - > They commonly involve RAG systems pulling from your knowledge base, agent orchestration coordinating multiple steps, and applicati
  - > integrated prompt environments – essentially admin versions of their actual user interface that expose prompt ed
  - > ecessary jargon.
  - > The engineers kept saying, “We’re going to build an agent that does XYZ,” when really the job to be done was writing a promp
  - > synthetic data. LLMs can generate realistic test cases that cover the range of scenarios your AI will encounter.
  - > test different tones of voice, levels of technical sophistication, or even different locales and languages. T
  - > A test database with enough variety to support your scenarios A way to verify that generated queries actually trigger intended scenarios
  - > synthetic data is grounding it in real system constraints. F
  - > When generating synthetic data, follow these key principles to ensure it’s effective:
  - > n other words, it is impossible to completely determine evaluation criteria prior to human judging of LLM outputs.
  - > So how do you build evaluation systems that remain trustworthy despite criter
  - > In contrast, a binary pass/fail forces evaluators to make a clear judgment: did this output achieve its purpose or not?
  - > While binary decisions provide clarity, they work best when paired with detailed critiques that capture the nuance of why something passed or failed. T
  - > If you’re using LLMs to evaluate outputs (which is often necessary at scale), it’s crucial to regularly check how well these automated evaluations align with human judgment.
  - > Start with high human involvement:
  - > focus on understanding where automated evaluations align with human judgment and where they diverge.
  - > use statistical techniques to sample outputs that provide the most informatio
  - > continue to compare automated evaluations against human judgment regularly
  - > At the top of the funnel is the most basic functionality – can the system respond at all? At the bottom is fully solving the user’s job to be done
  - > 1. Can generate syntactically valid queries (basic functionality) 2. Can generate queries that execute without errors 3. Can generate queries that return relevant results 4. Can generate queries that match user intent 5. Can generate optimal queries that solve the user’s problem (complete solution)
  - > they commit to a cadence of experimentation, learning, and iteration.
  - > having robust evaluation infrastructure
  - > most people don’t realize is that the team invested heavily in building sophisticated offline evaluation infrastructure.
  - > With solid evaluation in place, the team ran thousands of experiments, quickly identified what worked, and could say with confidenc
