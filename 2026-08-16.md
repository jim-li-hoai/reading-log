# Reading Log — 2026-08-16

- **13:29** [Demystifying evals for AI agents \ Anthropic](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)
  - > agents operate over many turns: calling tools, modifying state, and adapting based on intermediate results
  - > give an AI an input, then apply grading logic to its output to measure succes
  - > non-agentic evals were the main evaluation method
  - > —which means mistakes can propagate and compound.
  - > we run multiple trials t
  - > A task can have multiple graders, each containing multiple assertions
  - > the complete record of a trial, including outputs, tool calls, reasoning, intermediate results, and any other interactions.
  - > The outcome is the final state in the environment at the end of the trial
  - > It provides instructions and tools, runs tasks concurrently, records all the steps, grades outputs, and aggregates results.
  - > system that enables a model to act as an agent: it processes inputs, orchestrates tool calls, and returns results
  - > omes when users report the agent feels worse after changes, and the team is “flying blind” with no way to verify except to guess and check.
  - > Teams can't distinguish real regressions from noise, automatically test changes against hundreds of scenarios before shipping, or measure improvements.
  - > dimensions of a successful editing workflow: don’t break things, do what I asked, and do it well.
  - > In 3 months, they built an eval system that runs their agent and grades outputs with static analysis
  - > you get baselines and regression tests for free: latency, token usage, cost per task, and error rates can be tracked on a static bank of tasks.
  - > three types of graders: code-based, model-based, and human
  - > scoring can be weighted (combined grader scores must hit a threshold), binary (all graders must pass), or a hyb
  - > Capability vs. regression evals
  - > apability evals with high pass rates can “graduate” to become a regression suite that is run continuously to catch any drift.
  - > Two widely used coding agent benchmarks, SWE-bench Verified and Terminal-Bench, follow this approach
  - > gives agents GitHub issues from popular Python repositories and grades solutions by running the test suite; a solution passes only if it fixes the failing tests without breaking existing ones.
  - > a coding task where the agent must fix an authentication bypass vulnerability.
  - > In practice, coding evaluations typically rely on unit tests for correctness verification and an LLM rubric for assessing overall code quality,
  - > Unlike traditional chatbots, they maintain state, use tools, and take actions mid-conversation
  - > challenge: the quality of the interaction itself is part of what you're evaluating.
  - > outcomes and rubrics that capture both task completion and interaction quality.
  - > s the ticket resolved (state check), did it finish in <10 turns (transcript constraint), and was the tone appropriate (LLM rubric)
  - > multidimensionality are 𝜏-Bench and its successor, τ2-Bench.
  - > use model-based graders to assess both communication quality and goal completion,
  - > A benchmark like BrowseComp, for example, tests whether AI agents can find needles in haystacks across the open web—questions designed to be easy to verify but hard to solve.
  - > rather than through APIs or code execution.
  - > Browser use agents require a balance between token efficiency and latency.
  - > pass@k measures the likelihood that an agent gets at least one correct solution in k attempts.
  - > pass^k measures the probability that all k trials succeed.
  - > This metric especially matters for customer-facing agents where users expect reliable behavior every time.
  - > Begin with the manual checks you run during development—the behaviors you verify before each release and common tasks end users try.
  - > A good task is one where two domain experts would independently reach the same pass/fail verdict
  - > With frontier models, a 0% pass rate across many trials
  - > a known working output that passes all graders
  - > Test both the cases where a behavior should occur and where it shouldn't.
  - > The challenge was preventing the model from searching when it shouldn’t, while preserving its ability to do extensive research when appropriate.
  - > It’s essential that the agent in the eval functions roughly the same as the agent used in production
  - > Each trial should be “isolated” by starting from a clean environment.
  - > great eval design involves choosing the best graders for the agent and the tasks.
  - > We recommend choosing deterministic graders where possible, LLM graders where necessary or for additional flexibility
  - > So as not to unnecessarily punish creativity, it’s often better to grade what the agent produced, not the path it took.
  - > For tasks with multiple components, build in partial credit
  - > LLM-as-judge graders should be closely calibrated with human experts to gain confidenc
  - > and then grade each dimension with an isolated LLM-as-judge
  - > You won't know if your graders are working well unless you read the transcripts and grades from many trial
  - > When a task fails, the transcript tells you whether the agent made a genuine mistake or whether your graders rejected a valid solution. I
  - > Eval saturation occurs when an agent passes all of the solvable tasks, leaving no room for improvemen
  - > An eval suite is a
  - > st effective was establishing dedicated evals teams to own the core infrastructure, while domain experts and product teams contribute most eval tasks and run the evaluations themselves.
  - > early testing but fail to meet unstated expectations that a well-designed eval would have surfaced early
  - > Capability evals that start at a low pass rate make this visible. When a new model drops, running the suite quickly reveals which bets paid off
  - > Automated evals are especially useful pre-launch and in CI/CD, running on each agent change and model upgrade as the first line o
  - > /B testing validates significant changes once you have sufficient traffic
